# 虚拟交易功能说明

## 概述

虚拟交易功能允许您在不使用真实资金的情况下测试网格交易策略。系统会使用真实的市场数据进行模拟交易，帮助您验证策略的有效性。

## 功能特点

### ✅ 已实现的功能
- **真实市场数据**: 使用真实的行情数据、订单簿、K线等
- **虚拟资金管理**: 模拟买卖操作和余额变化
- **完整交易流程**: 包括订单创建、成交、余额更新等
- **策略测试**: 支持所有现有的网格交易策略
- **数据持久化**: 虚拟交易记录会保存到本地文件
- **实时监控**: 支持Web界面监控虚拟交易状态
- **交易统计**: 提供详细的盈亏统计和交易总结

### 🎯 核心特性
- **零风险**: 所有交易都是虚拟的，不会影响真实资金
- **真实环境**: 基于真实市场数据进行模拟
- **完整功能**: 支持所有网格交易功能（除理财外）
- **易于切换**: 通过配置文件即可在真实/虚拟模式间切换

## 配置方法

1. **复制配置文件**
   ```bash
   cp .env.simulation.example .env
   ```

2. **修改关键配置**
   
   在`.env`文件中设置以下配置项：
   ```env
   # 启用虚拟交易模式
   SIMULATION=true
   
   # 设置初始虚拟资金(USDT)
   SIMULATION_INITIAL_BALANCE=10000.0
   
   # 设置要测试的交易对
   SYMBOLS=BNB/USDT
   
   # 仍需要真实的API密钥(用于获取市场数据)
   BINANCE_API_KEY=your_api_key
   BINANCE_API_SECRET=your_api_secret
   ```

   **重要说明**：
   - `SIMULATION=true` 启用虚拟交易模式
   - `SIMULATION=false` 切换到真实交易模式
   - `SIMULATION_INITIAL_BALANCE` 设置虚拟账户初始资金
   - API密钥仍然必需，用于获取实时市场数据

3. **验证配置**
   ```bash
   python test_config.py
   ```
   这将显示当前的配置状态，确保虚拟交易设置正确。

4. **启动程序**
   ```bash
   python main.py
   ```

## 使用说明

### 启动提示
当启用虚拟交易模式时，程序会显示：
```
🎯 虚拟交易模式已启用
📊 初始虚拟资金: 10000.0 USDT
⚠️  注意：所有交易都是虚拟的，不会影响真实资金
```

### 交易通知
虚拟交易的通知消息会包含 `🎯 [虚拟]` 标识：
```
🟢 🎯 [虚拟] 买入 BNB/USDT
━━━━━━━━━━━━━━━━━━━━
💰 价格：600.00 USDT
📊 数量：0.0333 BNB
💵 金额：20.00 USDT
📈 网格：2%
```

### 数据存储
虚拟交易数据保存在 `data/simulation_data.json` 文件中，包含：
- 虚拟账户余额
- 交易历史记录
- 理财账户状态（虚拟）

### 交易总结
程序结束时会显示详细的交易总结：
```
🎯 虚拟交易总结报告
📊 总交易次数: 15
💰 初始资金: 10000.0 USDT
📈 当前资产分布:
   现货 USDT: 9500.0000
   现货 BNB: 0.8333
💹 总资产价值: 10050.00 USDT
📊 盈亏: +50.00 USDT (+0.50%)
```

## 注意事项

### ⚠️ 重要提醒
1. **API密钥仍需配置**: 虽然是虚拟交易，但仍需要真实的API密钥来获取市场数据
2. **无真实资金风险**: 所有交易操作都在虚拟环境中进行
3. **理财功能自动禁用**: 虚拟模式下不涉及真实的理财操作
4. **数据实时性**: 使用的是真实市场数据，价格波动与真实环境一致

### 🔄 模式切换
要从虚拟模式切换到真实交易：
1. 修改 `.env` 文件中的 `SIMULATION=false`
2. 重新启动程序
3. 确保账户有足够的真实资金

**配置格式说明**：
- 布尔值：`SIMULATION=true` 或 `SIMULATION=false`（注意小写）
- 数值：`SIMULATION_INITIAL_BALANCE=10000.0`（支持小数）
- 环境变量会被Pydantic自动类型转换

### 📊 性能测试
虚拟交易模式特别适合：
- 策略回测和验证
- 参数优化测试
- 新手学习和练习
- 长期策略效果观察

## 技术实现

### 架构设计
- **SimulationExchange类**: 包装真实交易所客户端，拦截交易操作
- **数据代理模式**: 市场数据直接从真实API获取
- **虚拟账户系统**: 独立的虚拟余额管理
- **状态持久化**: 自动保存和恢复虚拟交易状态

### 文件结构
```
├── simulation_exchange.py      # 虚拟交易所实现
├── data/
│   └── simulation_data.json   # 虚拟交易数据
├── .env.simulation.example     # 配置文件示例
└── data/
    └── trader_state_*.json    # 交易器状态文件
```

## 常见问题

**Q: 虚拟交易的价格是否准确？**
A: 是的，所有价格数据都来自真实的Binance API，与真实交易环境完全一致。

**Q: 可以同时运行多个交易对吗？**
A: 可以，在SYMBOLS配置中用逗号分隔多个交易对即可。

**Q: 虚拟交易数据会丢失吗？**
A: 不会，所有数据都会自动保存到本地文件中，重启程序后会自动恢复。

**Q: 如何重置虚拟账户？**
A: 删除 `data/simulation_data.json` 文件，然后重启程序即可重置为初始状态。

**Q: 虚拟交易是否支持所有功能？**
A: 支持几乎所有功能，除了涉及真实资金的理财操作会被自动跳过。